# PluginHost

Kind: symbol

## Members

- world
- overlay
- events
- installed
- (anonymous)
- install — (plugin: PluginDefinition): void
- uninstall — (id: string): void
- isInstalled — (id: string): boolean
- list — (): string[]
